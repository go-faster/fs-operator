# Configuration

<!-- TODO(P1/P2): fill per SPEC §13 — one section per spec field group. -->

Covers each `FSCluster` spec section: `image`, `scheme`, `topology` (flat
nodes vs racks, anti-affinity), `storage` (disks, weights, reclaim),
`etcd`, `clusterSecretRef`, `auth`, `s3` (service, TLS), `rebalance`,
`integrity`, `updatePolicy`, `observability`, `networkPolicy`,
`podTemplate`.

Until this guide is written, the [API reference](../reference/api.md) and
[examples](../../examples/) are the source of truth.
